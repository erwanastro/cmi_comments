<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}CMI Comments{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link href="/css/styles.css" rel="stylesheet" type="text/css"/>
</head>
<body>
    <div class="container">
        <div class="row user-banner">
            <div class="offset-md-9 col-md-3 offset-xs-8 col-xs-4">
                {% if app.user %}
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Welcome {{ app.user.name }}&nbsp;&nbsp;
                            <img src="{{ app.user.avatar }}" alt="My google avatar" class="avatar"/>
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('google_logout') }}">Logout</a></li>
                        </ul>
                    </div>
                {% else %}
                    <a href="{{ path('connect_google') }}" class="btn btn-dark" style="float:right">Login / register with Google</a>
                {% endif %}
            </div>
        </div>

        {% if error is defined %}
            <div class="row">
                <div class="col-md-12 col-xs-12">
                    <span class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                </div>
            </div>
        {% endif %}

        {% block content %}{% endblock %}

        <div class="footer"></div>
    </div><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>
